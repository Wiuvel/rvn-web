/**
 * Global types
 *
 * NOTE: Starting with vinext 1.0.0-beta, ambient module declarations for `next/*`
 * and `*.css` are natively provided via `@vinext/types` through `next-env.d.ts`
 * (`import "vinext/types";`). The previous workaround ambient declarations have been removed.
 */

export {};
